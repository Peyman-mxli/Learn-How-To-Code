# CLI Calculator Notes

This project practices:

- functions;
- dictionaries of callables;
- loops;
- input validation;
- exception handling; and
- separating reusable logic from the interactive entry point.

The arithmetic functions can be imported and tested without starting the CLI because execution is protected by:

```python
if __name__ == "__main__":
    main()
```
