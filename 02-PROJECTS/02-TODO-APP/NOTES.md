# Todo App Notes

This project introduces CRUD-style actions on an in-memory collection:

- Create → add a task
- Read → view tasks
- Update → mark a task complete
- Delete → remove a task

The state is intentionally stored in memory for this beginner project. Persistence with JSON or SQLite is a natural next extension.
