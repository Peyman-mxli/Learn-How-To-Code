# HTML in VS Code — Notes

HTML files use the .html extension. Create a project folder, add index.html, save, open it in a browser, inspect the DOM with DevTools, validate the markup, and commit the work. VS Code includes HTML language support and Emmet. Live Server is optional.
